extends layout

block content
    h1= title
    p Welcome to #{title}
    form(method="post",enctype="multipart/form-data",action="/")
        input(type="file",name="avatar")
        input(type="submit")
